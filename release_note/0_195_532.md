# v0.195.532 リリースノート

## 概要
設計・要件定義フェーズのバージョン。コード変更は未実装。以下4件の要件定義・設計書を作成。

## 計画中の要件

### RQ-1: Daily Notes 日付操作ボタンの左端移動
- 固定タグバー内で日付操作ボタン群を右端から左端（固定タグの前）に移動
- 対象: `notesWebviewContent.ts`, `outliner.css`

### RQ-2: 検索・スコープ状態の履歴移動機能
- 検索バー左端に「戻る」「進む」ボタン追加
- 検索条件 + Scope状態を履歴として記録・復元
- 独自スタック実装（History API不使用）
- 対象: `outliner.js`, `outliner.css`, `outlinerWebviewContent.ts`, `notesWebviewContent.ts`

### RQ-3: Sidepanel Copy Path ボタン追加
- markdown editor sidepanelヘッダーにファイルパスコピーボタン追加
- `navigator.clipboard.writeText` 使用
- 対象: `editor-body-html.js`, `editor.js`, `styles.css`

### RQ-4: フォーカスモード検索ヒットノードの祖先パンくず表示
- フォーカスモード検索結果の各マッチノード上部に祖先パンくず表示
- `renderFocusNodes()` 内で生成（`createNodeElement` 変更なし）
- 対象: `outliner.js`, `outliner.css`

## 設計ドキュメント
- `docs_dev/requirement-0_195_532.md` — 要件定義書
- `docs_dev/design-0_195_532.md` — 設計書
